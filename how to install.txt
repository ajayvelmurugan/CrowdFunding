Requirements:
  1. Install Node Js 
  2. Install Metamask Plugin in chrome or firefox browser
 
Steps to Install:
  1. Open Terminal in CrowdFunding Folder
  2. Type the following Command
          npm install
  3. Compile the contract using the following command
         node /ethereum/compile.js  
  4. Deploy the contract using the following command
         node /etherem/deploy.js
  5. Now copy the deployed address to factory.js file
  6. To run the contract type the following command
         npm run dev
  7. Open the browser and go to the following address
        localhost:3000
  
